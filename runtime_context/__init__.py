"""runtime_context — Runtime Context Layer (RCL) package.

Commit 2.1 introduces ONLY the Execution Evidence Bundle data contract
(execution_evidence_bundle.py). It has no consumer and no runtime integration.
This package marker intentionally imports nothing (no import-time side effects).
"""
