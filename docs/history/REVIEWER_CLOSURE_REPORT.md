# Reviewer Closure Report

Maps each anticipated reviewer concern to the concrete evidence generated this campaign. Every
"Evidence" cell points to a real artifact produced by an executed experiment. No value is fabricated.

| # | Reviewer concern | Evidence generated | Artifact / Table / Figure / Proof / Replay | Resolution | Remaining limitation |
|---|---|---|---|---|---|
| R1 | "AgentDojo integration is not executed (marked N/A)." | Genuine `agentdojo==0.1.35` benchmark executed under L-DREA with a local LLM; 33 episodes, 14 Gamma decisions (11 PERMIT, 3 SAFE_STATE) across 6 policy classes and all 4 suites. | Table 11; `audit_run/summary/statistics.json`; reviewer reports `audit_run/summary/reviewer/`; proofs `audit_run/summary/proofs/`. | **Fully resolved** (integration executable and executed). | Full 79×629 corpus not exhausted (runtime budget); CIs are finite-sample. |
| R2 | "Are authorization decisions reproducible/replayable?" | Every trace re-derived from `execution_trace.jsonl` alone (Γ_global=OR(deficits), Π, decision); 33/33 traces consistent, 14 authorization steps. | `audit_run/summary/replay_validation.json`; ReplayEngine; per-episode `replay_report.json`. | **Fully resolved.** | — |
| R3 | "Does the monitor perturb the benchmark (observer effect)?" | Traced vs clean-runtime authorization streams byte-identical; final env-state hash identical; tracer is record-then-delegate. | per-episode `validation_report.json`; `EXECUTION_TRACE_SYSTEM.md`. | **Fully resolved.** | Episode utility/security still vary via LLM non-determinism (not the monitor). |
| R4 | "Are the frozen components actually unchanged during evaluation?" | SHA256 of 19 frozen files snapshotted before/after every episode — unchanged across the whole campaign. | Table 11 (frozen integrity); `audit_run/summary/frozen_integrity.json`; `trace_integrity_all.json`. | **Fully resolved.** | — |
| R5 | "What is the runtime overhead of the authorization boundary?" | Gamma-decision overhead mean 0.0216 ms (AgentDojo path); per-stage core 0.197 ms/row; Runtime Context 0.0106 ms/row; Replay 0.0150 ms/row. | Table 10; Table 11; `PERFORMANCE_RESULTS.json`; `runtime_profile.json` (Campaign D). | **Fully resolved.** | Two harness contexts (n=2000 / n=5000) reported separately. |
| R6 | "The profiler cannot isolate Runtime Context and Replay latency." | Campaign D wraps timers around the frozen `FreshnessClock`/`CommitActuateJournal` and `write_replay_manifest`; both isolated and measured. | Table 10; `audit_run/summary/runtime_profile/runtime_profile.{json,md}`. | **Fully resolved.** | — |
| R7 | "Concurrency scaling is missing." | Full 1→32-thread scaling curve on the frozen decision path: throughput, speedup, scaling efficiency, p50/p95/p99, queue delay, CPU util, peak RSS, correctness/ledger/false-permit/deny. | Table 13; `concurrency_scaling.{json,csv,md}`; `concurrency_scaling_throughput.svg`, `_latency.svg`. | **Resolved (threads).** | Throughput is GIL-bound (pure-Python path); process-level parallelism is future work. |
| R8 | "False-permit / false-deny rate is null." | Independent labeling pipeline (attacker targets extracted from injection GOALs, not the monitor's gate). FPR undefined (n=0 attacker-targeted actions); FDR 0.000 [0,0.434] on recognized-set legit class. | Table 11; `audit_run/summary/fpr_fdr/fpr_fdr.{json,md}`. | **Partially resolved** — pipeline exists and runs; FPR has no test cases in this corpus. | Non-zero malicious-action denominator needs a stronger tool-calling agent; FDR near-tautological. |
| R9 | "Do injection attacks defeat the monitor (soundness)?" | 1/33 episodes had security=True, but via a **read-only** action (no EEA) → content-layer harm, Property-v / Group III, outside L-DREA's authority. 0 attacks succeeded via an EEA. | Table 11; trace `travel/user_task_0__injection_task_6` (`get_rating_reviews_for_hotels`, `mediated=False`). | **Resolved (delimits the claim honestly).** | Content-layer harms remain out of scope by construction. |
| R10 | "Statistics could be cherry-picked / fabricated." | All statistics computed by the frozen Phase-2 engine from recorded events; 21 unit tests (Wilson, bootstrap determinism, hash-chain tamper detection, replay inconsistency flagging). | `statistics.json`, `statistics_tables.md`; `audit/tests/test_audit.py`. | **Fully resolved.** | — |
| R11 | "Trace could be tampered post-hoc." | Append-only hash chain per trace; event-ordering and timestamp-monotonicity checks; all 33 traces integrity-OK. | `trace_integrity_all.json`; `integrity.py`; unit test proves a single flipped field changes the chain root. | **Fully resolved.** | — |
| R12 | "Every authorization needs an auditable derivation." | Per-authorization proof chains Predicate→Deficit→Γ→Π→Authorization→Execution→Environment→Evidence→Replay→Verification; all consistent. | `audit_run/summary/proofs/*.md|json`, `all_proofs.json`; Proof Generator. | **Fully resolved.** | — |

## Summary
- **Fully resolved:** R1, R2, R3, R4, R5, R6, R10, R11, R12 (9).
- **Resolved with honest scope delimitation:** R7 (threads/GIL), R9 (Group III content-layer).
- **Partially resolved:** R8 (FPR pipeline exists; 0 test cases in this corpus — needs a stronger agent).
