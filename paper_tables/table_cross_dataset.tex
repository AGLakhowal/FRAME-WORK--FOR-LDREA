% Auto-generated by generate_paper_tables.py from executed artifacts. Do not edit by hand.
% Requires: \usepackage{booktabs}
\begin{table*}[!t]
\centering
\scriptsize
\caption{The complete combined ablation executed independently on every real dataset. Datasets are not merged and metrics are not normalised across them: prevalence and the observable feature space differ by design, so the absolute rates are not comparable. What replicates is the ablation STRUCTURE.}
\label{tab:cross-dataset}
\begin{tabular}{llrrrrrrrrr}
\toprule
Dataset & Domain & Rows & Prev. & Preds & BlindAcc & URR & Recall & RIS & URR (--PE) & Evid. (--EQ) \\
\midrule
UNSW-NB15 & network intrusion telemetry & 20,000 & 55.78\% & 7 & 0.755 & 0.339 & 0.661 & 1.000 & 1.000 & 0.000 \\
ULB & financial (PCA-anonymised) & 20,000 & 0.43\% & 5 & 0.976 & 0.061 & 0.939 & 1.000 & 1.000 & 0.000 \\
IEEE-CIS & financial (transactions) & 20,000 & 2.81\% & 6 & 0.874 & 0.658 & 0.342 & 1.000 & 1.000 & 0.000 \\
\bottomrule
\end{tabular}
\\[2pt]
\begin{minipage}{\textwidth}\footnotesize Columns 6--9 are the full-stack baseline on that dataset. The last two columns are the ablated results that carry the finding: removing the predicate engine (--PE) drives the undetected-risk rate (URR) to 1.000 on \emph{every} dataset, and removing the evidence quad (--EQ) zeroes evidence completeness and cascades the ledger to undefined on every dataset. Ablating the audit plane (EQ/LG/HC) leaves the undetected-risk rate (URR) bit-identical to the baseline, confirming the ledger is strictly downstream of the authorisation decision. Conclusions replicate on all datasets: \checkmark. $\Gamma$ is imported and used identically for every dataset. \textbf{Note --- URR is not the False Permit Rate.} $\mathrm{URR}\;(\text{Undetected Risk Rate}) = \mathrm{FN}/(\mathrm{TP}+\mathrm{FN}) = 1 - \text{Blind Detection Recall}$. This metric measures the fraction of malicious events that remain undetected during blind runtime evaluation. It is \emph{not} the False Permit Rate reported in the main authorization benchmark. The paper's False Permit Rate ($0/492$ and $0/62$) measures authorization soundness and remains unchanged.\end{minipage}
\end{table*}
