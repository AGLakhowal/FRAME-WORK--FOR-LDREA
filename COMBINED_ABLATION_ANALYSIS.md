# Combined Component Ablation — Scientific Interpretation

> Auto-generated by `experiment_combined_ablation.py`. Every quantitative claim cites a value measured by executing the full L-DREA runtime. Nothing is analytically estimated.

- Configurations: **19** (baseline + 5 singles + 10 pairs + 2 triples + full), n=6000/config, run in 120.25s.
- Components auto-discovered: **9** (PE, RV, EQ, LG, HC, RD, WD, FT, CK); execution order PE → RV → EQ → LG → HC → WD → FT → CK → RD.
- Baseline RIS **1.000** (=1.0 intact); baseline absolute risk-detection recall **0.481** (below 1.0 because ~40% of synthetic fraud is stealthy by construction — a generator property, normalized out of RIS).

## Most / least important component

**Most important on the security axis: remove_PE** — FPR 0.519→1.000, recall 0.481→0.000. **Most important on the audit axis: remove_EQ** (mean audit-plane health 0.000) — it cascades evidence→ledger→hash-chain→replay to 0.
The least individually impactful removals on RIS are the security-plane singles (remove_PE, remove_RV), each costing only one of six health planes — but note this is an artefact of RIS being audit-weighted; on the security axis those same removals are catastrophic (see FPR).

## Largest interaction

The largest interaction effect is **remove_EQ+HC+LG+PE+RV** (observed Δ(RIS) 1.000 vs additive 1.998, interaction -0.998, Redundant (saturated)). Planes: EQ (evidence plane); HC (ledger plane); PE (authorization plane); LG (ledger plane); RV (enforcement plane). Dependency within the set: HC depends on LG, HC depends on EQ, LG depends on EQ. The single removals predict a degradation exceeding the whole stack (additive > baseline 1.0), but integrity floors at 0 — the combination is saturated, not mutually redundant on independent planes.

## Critical dependencies

- **remove_EQ+HC** — observed Δ(RIS) 0.667 ≈ upstream single, not the sum (1.164). Planes: EQ (evidence plane); HC (ledger plane). Dependency within the set: HC depends on EQ. The upstream component is a prerequisite of the downstream one, so the downstream integrity was already destroyed by the upstream removal alone — the combined effect equals the upstream single effect, not their sum.
- **remove_EQ+HC+LG** — observed Δ(RIS) 0.667 ≈ upstream single, not the sum (1.664). Planes: EQ (evidence plane); HC (ledger plane); LG (ledger plane). Dependency within the set: HC depends on LG, HC depends on EQ, LG depends on EQ. The upstream component is a prerequisite of the downstream one, so the downstream integrity was already destroyed by the upstream removal alone — the combined effect equals the upstream single effect, not their sum.
- **remove_EQ+LG** — observed Δ(RIS) 0.667 ≈ upstream single, not the sum (1.167). Planes: EQ (evidence plane); LG (ledger plane). Dependency within the set: LG depends on EQ. The upstream component is a prerequisite of the downstream one, so the downstream integrity was already destroyed by the upstream removal alone — the combined effect equals the upstream single effect, not their sum.
- **remove_HC+LG** — observed Δ(RIS) 0.500 ≈ upstream single, not the sum (0.998). Planes: HC (ledger plane); LG (ledger plane). Dependency within the set: HC depends on LG. The upstream component is a prerequisite of the downstream one, so the downstream integrity was already destroyed by the upstream removal alone — the combined effect equals the upstream single effect, not their sum.

## Redundant components

- **remove_EQ+HC+LG+PE+RV** (Redundant (saturated)): Planes: EQ (evidence plane); HC (ledger plane); PE (authorization plane); LG (ledger plane); RV (enforcement plane). Dependency within the set: HC depends on LG, HC depends on EQ, LG depends on EQ. The single removals predict a degradation exceeding the whole stack (additive > baseline 1.0), but integrity floors at 0 — the combination is saturated, not mutually redundant on independent planes.
- **remove_EQ+PE+RV** (Redundant (saturated)): Planes: EQ (evidence plane); PE (authorization plane); RV (enforcement plane). The single removals predict a degradation exceeding the whole stack (additive > baseline 1.0), but integrity floors at 0 — the combination is saturated, not mutually redundant on independent planes.

Redundancy is **structural, not wasteful** — HC and LG share the tamper-evidence asset and EQ is upstream of both; no single removal is loss-free (each degrades ≥1 plane).

## Synergistic combinations

None measured — the two security controls act on independent rate metrics, so combined removal is additive (reported as measured, not assumed).

## Graceful degradation

The architecture degrades gracefully on the audit plane but not the security plane. 7 configurations are `AUDIT-DEGRADED` (lose provenance while FPR stays at baseline 0.519). Removing PE is deliberately not graceful — recall →0.000, FPR→1.000. See GRACEFUL_DEGRADATION_ANALYSIS.md.

## Overall architectural robustness

Worst multi-component configuration: **remove_EQ+PE+RV** (RIS 0.000). Indispensable components (single removal collapses ≥1 plane to 0): PE, RV, EQ, LG, HC. No component is wasteful: every single removal is a measured, statistically-tested regression (see Table B and per-config statistics).

## Reproduce

```bash
python3 experiment_combined_ablation.py
```

